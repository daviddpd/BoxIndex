# Sample Data

The sample import bundle in this folder is meant for screenshots and app testing.

- Import the entire `BoxIndex Household Demo Export` folder in BoxIndex if you want the container photos to import too.
- If you import only `boxindex-export.json`, the containers and items will import, but the attachments will not.
